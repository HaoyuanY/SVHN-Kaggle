1. use kera.py to get train model, I already trained it(mymodel1.h5)
2. use predict.py to get the prediction
3. use fix.py to get the real result
4. don't forget to replace 0 with 10

Note, the dataset path is changed, you may want to look at the util.py